# fluidbox — Session Handover

**Date:** 2026-07-10 (rev 7: borrow-the-agent Phase 5 shipped) · **State:** M0 + M1 + §6.A hardening + **connected git workspaces (Phase 1)** + **generic API borrowing & signed callbacks (Phase 2)** + **scheduled borrowing (Phase 3)** + **GitHub PR-review fan-out on the connector seam (Phase 4)** + **capability & MCP catalog (design-doc Phase 5)** done; `just e2e` = 306 checks green across 8 phases.

Read `CLAUDE.md` (commands + invariants + gotchas) and `PLAN.md` (authoritative design + roadmap) alongside this. This doc is the "where we are right now / how to pick up" note.

---

## 1. Current status: what works

M1 (the local-first governed MVP) is **done and proven live**, not just compiled:

- **Full run lifecycle** `created → provisioning → initializing → running ↔ awaiting_approval → completed|failed|cancelled|budget_exceeded`, server as single status writer.
- **Live agent demo (demo A)** ran from both the CLI and the dashboard: the Claude Agent SDK agent provisioned a sandbox, ran a failing test, diagnosed the `multiply` bug, edited the file, re-ran to confirm, and completed — clean diff + accurate cost. Isolation confirmed (original repo untouched; only the sandbox copy changed).
- **Governance plane (demos B & C mechanics)** proven via `scripts/governance-e2e.sh` — 14/14 checks over real HTTP: policy allow/deny, approval pause→resume, `tool_call_id` idempotency, autonomous instant-deny with the ledger recording `source=autonomy_rewrite` + `original_verdict`.
- **LLM gateway** (LiteLLM, pinned by digest) + Rust facade with SSE tee metering; the sandbox's fake `ANTHROPIC_API_KEY` is its session token, real key isolated to the gateway.
- **Dashboard** (Next.js 16, control-room UI): Operations, New Run, live SSE timeline, agents registry + add-revision, approvals inbox, policies YAML editor + validate, settings/health.
- **Connected git workspaces (design-doc Phase 1, shipped 2026-07-10):** `WorkspaceSpec` (scratch | local_copy | git_repository) evolved from `RepoSource` with wire-compat aliases; GitHub **integration connections** (PAT validated against the GitHub API, AEAD-sealed at rest via `FLUIDBOX_CREDENTIAL_KEY`, never in any response/RunSpec/sandbox/ledger); repository picker (`GET /v1/connections/{id}/repos`); optional **default workspace on agent revisions** with resolution precedence explicit > revision default > scratch; control-plane-side clone of exact ref/commit (`materialize_git` — creds via `GIT_CONFIG_*` env only, origin remote removed, shallow + full-fetch fallback for SHAs); per-session workspace **cleanup after diff capture** (idempotent; `FLUIDBOX_KEEP_WORKSPACES=1` to keep); Connections page + workspace pickers in the dashboard; CLI `--git-url/--git-ref/--git-commit/--connection`.
- **Generic API borrowing + signed result callbacks (design-doc Phase 2, shipped 2026-07-10):** `trigger_subscriptions` (migration 0003) as the standing borrow instruction — **§17 #6 settled: caller task/workspace overrides are opt-in per subscription, both default OFF** (`allow_task_override`, `allow_workspace_override`); **scoped trigger tokens** (`api_tokens.kind='trigger'`, sha256-hashed, `fbx_trig_` prefix, rotation via `POST /v1/triggers/{id}/rotate_token`) that can invoke exactly one subscription + poll its runs and nothing else; **one unified `run_service::create_run`** every entry point (manual UI/CLI + API trigger) converges on, freezing `InvocationContext` (kind=manual|api, also written to `sessions.trigger`) and `result_destinations` into the RunSpec with wire-compat defaults for old rows; `POST /v1/triggers/{id}/invoke` with **Idempotency-Key** (claim-bind-release on `trigger_invocations`: replay returns the same run, body drift → 422, concurrent claim → 409, stale unbound claims stealable after 60s); strict `{{key}}` task templating from caller context; workspace narrowing (repo swap only within a github/connection base, ref/commit pins — never a new connection/URL/path); **signed webhook callbacks** — `result_deliveries` enqueued inside the single `orchestrator::transition` funnel on terminal entry, worker signs `x-fluidbox-signature: v1=hmac-sha256(secret, "{ts}.{body}")` with per-subscription AEAD-sealed secrets (shown once at creation), 6-attempt backoff 5s→1h, `callback.delivered/failed` ledger events, and a completed run stays completed when the destination is dead; scoped polling `GET /v1/triggers/{id}/runs/{sid}`; dashboard Triggers page (create + show-once token/secret + curl example, enable/disable, rotate, per-subscription runs & deliveries) plus invocation chip + deliveries panel on the session page.
- **Scheduled borrowing (design-doc Phase 3, shipped 2026-07-10):** a schedule is a **trigger subscription with a clock, never a new object** — `schedules` table (migration 0004: cron, **explicit IANA timezone** for DST-correct next-fire, `next_fire_at`, `missed_run_policy`, `last_fired_at`; one per subscription, enabled-ness rides the subscription) + a 1s tick worker (`scheduler.rs`, shaped like `deliveries.rs`) firing through the same `run_service::create_run` with `InvocationContext.kind=schedule` (cron/timezone/fire_time/catch_up frozen into the RunSpec). **Exactly-once firing**: each fire time claims a deterministic key (`sched:{fire_time}`) on the Phase 2 `trigger_invocations` table, and `create_session` binds the claim **in the same transaction** as the session insert — a crashed/restarted or double-fired scheduler replays, never duplicates. **§17 #5 settled (2026-07-10): overlap default `allow`, missed-run default `skip`; `concurrency_policy` lives on `trigger_subscriptions` and is enforced in `create_run` for ALL invocations** — `skip_if_running` records a visible skip (claim row `skip_reason='overlap'`; API invokes get 409), `replace` cancels the active run (reason names the replacement) then starts fresh. Missed gaps (scheduler down or subscription disabled — same path) record ONE skip keyed at the oldest missed fire time, or fire exactly ONE `catch_up` run — never fire-all-missed. Config-time validation (cron/tz parse, template must render from `{{fire_time}}` alone, policies enumerated); pure cron domain in `fluidbox-core/src/schedule.rs` (`cron` + `chrono-tz`, 5-field normalized, 6-field seconds = the e2e fire-fast seam). Dashboard: schedule line on trigger rows (cron/tz/next/last fire, missed policy, concurrency chip), firings-&-skips column, schedule create form.
- **GitHub PR-review fan-out on the connector seam (design-doc Phase 4, shipped 2026-07-10):** the CONNECTOR SEAM is the feature; GitHub is its first tenant. One **provider-ignorant event spine** (`events.rs`: ingress → verify → normalize → match → `create_run` → publish — the e2e greps it to prove no provider name appears) with ALL GitHub knowledge in **one connector module** (`connectors/github.rs` behind the plain-match dispatch in `connectors/mod.rs`; no SDK/trait registry — §17 #8 n=1 discipline; Slack later = one new module + one `ResultDestination` variant). **Two-level DB-unique idempotency** (migration 0005): `trigger_deliveries` unique(connection, external_event_id) + `trigger_dispatches` unique(delivery, subscription), the dispatch bound to its session in the same transaction — webhook retries *heal* partial fan-outs, never duplicate runs or comments. **GitHub App connections** (`provider=github_app`: app id + installation id + RS256 private key + webhook secret, all validated then AEAD-sealed; installation tokens minted+cached ~1h; PAT connections stay for fetch); ingress `POST /v1/ingress/github/{connection_id}` is deliberately unauthenticated — the `X-Hub-Signature-256` HMAC against the sealed secret IS the auth, and nothing is stored before it verifies. **Fan-out**: one PR event → one run per matching subscription (event filter + repository selector, fail-closed matcher), each frozen at the **exact head SHA** with `InvocationContext.kind=event` (provider/external_event_id/event_type/resource frozen for audit); §17 #5 concurrency policies govern event dispatch too. **Fork trust tier is real**: head repo ≠ base repo (or hidden — fails toward fork) freezes `TrustTier::ReadOnly` + `checkout_mode=read_only`, enforced at the permission gate by `policy::read_only_denial` (allowlist: read tools + metachar-free read-only shell) ABOVE policy/subscription/human approval — no approval escape; denials ledger as `source=trust_tier`. **Publishers** ride the Phase-2 `result_deliveries` worker as new `ResultDestination` variants: `github_pr_comment` (one stable comment per (subscription, PR) via `external_results`, PATCHed in place on later events — §17 #3; recreated only if deleted externally) and `github_check` (one run per head SHA named `fluidbox/<subscription>`), both under the **App identity** (§17 #1) with per-agent attribution in the content; one agent's failure shows only on its own comment/check. **§17 #1–#3 settled 2026-07-10** (recorded in the design doc): App-only; default events `opened`+`reopened` with `synchronize` per-subscription opt-in (cost amplifier); update-in-place. Dashboard: App connect form + ingress URL on connection rows, event-subscription form (events, repos, publish modes) + event badges. E2E needs no public URL: fake GitHub API via `FLUIDBOX_GITHUB_API_URL` + `file://` clone base via `FLUIDBOX_GITHUB_CLONE_BASE`.
- **Capability & MCP catalog (design-doc Phase 5, shipped 2026-07-10):** the LAST of the four optional run inputs. **EXACTLY two tool classes — the split IS the security model (§8.3):** *sandbox* MCP servers (stdio subprocesses packaged in the runner image — `servers/workspace-info.mjs` is the first; credential-free by construction) and *brokered* MCP servers (the control plane speaks MCP Streamable HTTP via `broker.rs` and turns the SEALED credential server-side — the fourth instance of the credential inversion after the LLM facade, git fetch, and webhook verify). **Research-first:** the current MCP ecosystem was surveyed fresh (spec 2025-11-25 + the session-less 2026-07-28 RC; registry identity/digest practice; tool-poisoning/rug-pull/results-injection attack classes; common server profiles) and folded into the schema — findings note at `docs/research/2026-07-10-mcp-ecosystem-findings.md`; the honest read: the photograph rule categorically beats *drift/rug-pulls*, while attach-time poisoning is met by a registration lint (ANSI/zero-width/bidi rejection) and results-injection remains the gate+approvals' job. **Registry:** `capability_bundles` (migration 0006), append-only like agent revisions — publish = new (tenant, name, version) row; **registration IS the photograph** (brokered tools discovered via tools/list with the sealed bearer, sandbox tools declared; both digest-anchored: per-server `tools_digest` + bundle `definition_digest`, our SEP-1766-equivalent since npm/pypi registry entries carry no content hash). **§17 #7 settled (pin-only):** attaching `"name"` resolves the newest version AT ATTACH TIME and stores the exact pin; upgrade = append an agent revision; nothing floats. **§17 #4 explicitly deferred** (the gateway shipped; the git-write op list settles at its own boundary). **Freeze:** `run_service::create_run` intersects revision pins ∩ subscription keep-list ∩ per-run keep-list (remove-only §3.5), rejects server-alias collisions (shadowing defense), fails closed on inactive brokered connections at zero spend, strips everything on `TrustTier::ReadOnly` (fork PRs run with zero MCP surface), and freezes full snapshots into `RunSpec.capabilities` (+ `capability.frozen` ledger event). **The ONE gate** (`internal.rs::decide_tool_call`, shared by `/permission` and the new `/internal/sessions/{id}/tools/call`): budget → **frozen-set availability** (`source=capability` denials; a tool the live server later advertises simply doesn't exist — rug-pull dead) → trust tier → policy → approvals. **Attach ≠ allow** proven: an attached tool still gets policy-denied. **Broker flow:** the runner's canUseTool auto-allows brokered `mcp__*` tools because the broker endpoint runs the identical gate (same tool_call_id, idempotent approvals) before executing — one decision per call, always server-side; a runner that skips its callback changes nothing. Ledger per brokered call: `tool.requested` → `tool.decision` → `tool.brokered` (ok/latency_ms/result digest — never inputs, outputs, or secrets). **Credentials:** new `mcp_http` connection flavor (sealed bearer + `base_url`); the broker sends the credential only to URLs under the base (audience binding, our RFC-8707 equivalent). **Runner:** `FLUIDBOX_CAPABILITIES` env carries a stripped manifest (sandbox launch specs + brokered tool snapshots — no upstream URLs/connection ids); brokered servers mount as `broker-shim.mjs`, a stdio MCP server that advertises the FROZEN snapshot verbatim and forwards intents to the internal gateway (harness-agnostic: a Codex runner reuses it). Dashboard: Capabilities page (list/register with discovery), bundle pins on agent revisions + attach fields, trigger keep-list, frozen-capabilities panel + `tool.brokered`/`capability.frozen` timeline rendering.
- **Quality bar:** `cargo clippy --workspace -D warnings` clean; 98 unit/integration tests green; **`just e2e` = 306 acceptance checks green** (9 live demo A + 14 governance + 26 git workspaces + 50 api triggers + 54 scheduled borrowing + 61 github fan-out + 70 capability catalog + 22 failure-path). ~14,800 lines of Rust across 5 crates.

## 2. What's running right now (dev environment)

| Service | Where | Notes |
|---|---|---|
| fluidbox-server | host, `:8787` | `cargo run -p fluidbox-server`; bound `0.0.0.0` |
| dashboard | host, `:3000` | `pnpm dev` in `apps/web` |
| LiteLLM gateway | docker `deploy-litellm-1`, `:4000` | holds the real Anthropic key; pinned by digest in `.env` |
| Neon Postgres | cloud project `fluidbox` | endpoint `ep-fancy-recipe-a6lovnla` (us-west-2) |

To restart from cold: `just gateway-up` then `just dev` (needs `.env` populated, incl. `ANTHROPIC_API_KEY`).

Seeded state: agent `claude-fixer` is on **rev 4 = haiku** (current, so new runs are cheap); opus rev 3 preserved in history. Default policy seeded from `policies/default.yaml`. A `test-seq-agent` exists as a leftover from the db integration test (harmless).

## 3. Locked decisions (don't re-litigate without reason)

- **North star** (PLAN.md §2): agent-registry platform. The MVP is the final architecture at n=1 (one harness, one provider, one bundle) — never mock scaffolding. Every milestone must preserve the §2 convergence invariants.
- **Model gateway = LiteLLM behind a thin Rust facade** (not a hand-built proxy). Facade upstream is `LLM_UPSTREAM_URL` → direct-Anthropic + in-facade tee metering is the one-line fallback.
- **Autonomy from M1**: `supervised | autonomous` on the RunSpec; autonomous rewrites `RequireApproval` to the policy fallback (default deny), `canUseTool` always wired.
- **GitHub events ship as the first tenant of the connector seam** (Phase 4): the spine (router/matcher/dedup/publish plumbing) is provider-ignorant; ALL GitHub knowledge lives in `connectors/github.rs`. §17 #1–#3 settled: App-only identity, opened+reopened defaults (synchronize opt-in), update-in-place results. No public connector SDK until a second substantially different service validates the boundary (§17 #8).
- **Capabilities are EXACTLY two tool classes** (Phase 5, §8.3): sandbox (in-image stdio, credential-free) and brokered (control-plane MCP client, sealed credential server-side). No arbitrary lifecycle hooks. Attach ≠ allow; the photograph rule (registration-time snapshots, frozen pins, gate-side availability) is the drift/rug-pull defense — and deliberately NOT oversold: attach-time poisoning is met by the registration lint, results-injection stays the policy gate + approvals' job (see the findings note). **§17 #7 settled: pin-only** — upgrades append agent revisions; no floating refs. **§17 #4 explicitly deferred** past Phase 5.
- **Runtime:** Docker now; Lambda MicroVMs are M2. **Harness:** Claude Agent SDK now; Codex is M3 (the capability catalog it needed shipped in Phase 5).

## 4. Known rough edges (small, non-blocking)

- **New Run modal button toggles on each click** — a double-click opens-then-closes it. Cosmetic; make the header button open-only if it annoys.
- **Add-revision can't clear a system prompt** — omitting it inherits the previous revision's prompt (there's no "set to none" path). Minor API gap.
- **No delete for agents/policies/sessions** — registry only grows in the UI. Fine for M1.
- **LiteLLM runs under amd64 emulation on this arm64 Mac** — works, slightly slower boot. A native arm64 tag or `platform` override would speed it up.
- **GitHub App connections take pasted identity** (app id / installation id / private key / webhook secret) — no App-manifest or OAuth create-flow yet; you create the App in GitHub once, by hand (§5 has the recipe). Webhook secrets aren't rotatable (like callback secrets): recreate the connection.
- **Event ingress has no manual redeliver endpoint** — GitHub's own "Redeliver" button covers it (retries heal, never duplicate). The installation-token cache is in-memory per server; a restart just re-mints.
- **Fork-PR head SHAs fetch shallow-by-SHA on github.com** (`allowAnySHA1InWant`); plain git servers fall back to a full branch fetch, which may not contain fork commits — fork support is effectively GitHub-only, which is fine: it's the GitHub connector.
- **`external_results` rows live forever** (one per subscription×PR) — no cleanup on PR close; harmless at this scale.
- **`checkout_mode: read_only` is frozen intent only** — every checkout is a writable copy by construction; trust tiers keying off it come with fork-PR handling.
- **Repo picker lists the first 100 repos by update time** (`per_page` capped at 100, `page` param supported); no search box yet.
- **Trigger subscriptions are create/enable/disable/rotate only** — no edit or delete. To change a template or flags, create a new subscription. Fine at this scale; a PATCH comes when someone actually needs it.
- **Callback secrets are shown exactly once** and are not re-showable or rotatable (only the *token* rotates). Losing the secret = recreate the subscription.
- **`concurrency_policy` / `resource_selector` / `event_filter` columns are deliberately absent** from `trigger_subscriptions` — they land with the phases that enforce them (3: schedules, 4: webhooks). Store-but-ignore config would lie.
- **Callback delivery is at-least-once** — receivers should dedup on `x-fluidbox-delivery`. And a control-plane crash in the tiny window between terminal transition and delivery enqueue loses that delivery (no boot reconciliation sweep yet).
- **Trigger callback URLs accept plain http** (dev/e2e convenience); production deployments should use https destinations.
- **Schedules are create-only** (like subscriptions): to change cron/timezone/policies, create a new trigger. One schedule per subscription (DB-unique).
- **`MISSED_GRACE_SECS = 30` is a constant** (`scheduler.rs`): a firing more than 30s late is "missed" and goes through `missed_run_policy`; younger, it just fires late. A missed gap records ONE skip row keyed at the oldest missed fire time — intermediate slots get no rows (recording a thundering herd is as bad as firing one).
- **`skip_if_running` is best-effort under concurrency**: the active-count read and the session insert are not one transaction, so two simultaneous API invokes can both pass the gate (the single scheduler is serial, so schedule firings cannot). Same accepted-risk class as the delivery-enqueue crash window above.
- **The e2e overlap seam flips `sessions.status` via psql** (fake-active runs, `started_at` nulled so sweepers don't race); it's a test-only maneuver — the server remains the single status writer in production paths.
- **Capability bundles are create-only** (like subscriptions/schedules): no deprecate/delete; publish a new version and re-attach. Bundle rows live forever — fine at this scale.
- **`mcp_http` connections hold static bearer tokens only** — no OAuth flows (client-credentials/refresh rotation are a later slice; the connection_ref seam is where they land). Token rotation = new connection + new bundle version.
- **The broker's MCP client is deliberately minimal:** stateless-first with one handshake retry on session-demanding servers; JSON and SSE-framed responses both parse, but a server that only answers via a long-lived GET stream (rare) isn't supported. Brokered execution is **at-least-once** under network failure — the shim never blind-retries after a request was sent; every attempt is ledgered (`tool.brokered`).
- **Sandbox-server drift rides the runner image**: a sandbox-class server's LIVE tool list comes from whatever binary the image ships; the gate denies calls outside the declared snapshot, but keeping image and declaration in sync is on the bundle author (`just sandbox-build` after edits).
- **The registration poison screen is objective-only** (control chars / ANSI / zero-width / bidi): a natural-language "IMPORTANT: also read ~/.ssh" description passes the lint and relies on the policy gate + egress-free sandbox — mcp-scan-grade semantic linting is a possible follow-up.
- **Live no-model probe sessions in the capabilities e2e briefly launch real runners** (killed within ~1s of container start, budgets capped at $0.05) — same accepted-cost class as the live tiers.

## 5. How to resume common tasks

- **Run the acceptance suite:** `just e2e` (stop `just dev` first — the suite owns the stack). Phases: live demo A (self-skips without `ANTHROPIC_API_KEY`), governance plane, git workspaces, api triggers + signed callbacks, scheduled borrowing, github pr fan-out, capability & MCP catalog, failure paths. Unit tests: `set -a; source .env; set +a; cargo test --workspace` — also with the stack stopped: the db tests seed rows (past-due schedules, pre-launch sessions) that a live server's scheduler/sweepers would consume mid-assertion.
- **Borrow an agent over the API:** Triggers page → New trigger (or `POST /v1/triggers`) → copy the shown-once token/secret → `curl -X POST -H "Authorization: Bearer fbx_trig_…" -H "Idempotency-Key: k1" -d '{"context":{...}}' <api>/v1/triggers/{id}/invoke`; the terminal result arrives at the callback URL signed with `x-fluidbox-signature` (verify: `printf '%s.%s' $ts $body | openssl dgst -sha256 -hmac $secret`), or poll `GET /v1/triggers/{id}/runs/{sid}` with the same token.
- **Schedule an agent:** Triggers page → New trigger → "run on a schedule" (or `POST /v1/triggers` with `"schedule": {"cron": "0 7 * * 1-5", "timezone": "America/New_York", "missed_run_policy": "skip"}` + optional `"concurrency_policy": "skip_if_running"`). The template renders `{{fire_time}}`; firings, skips (overlap/missed), and next/last fire show on the trigger's row + activity panel. Disable pauses the clock; re-enable goes through the missed-run policy.
- **Edit the seed policy:** change `policies/default.yaml` **and** the `seed_policy_semantics` test that pins it, then `just policy-sync` to push it to the running control plane (version++; in-flight runs keep their frozen snapshot).
- **Wire a repo's PRs to agents (real GitHub, manual pass):** create a GitHub App in your org (permissions: Contents read, Pull requests write, Checks write; subscribe to Pull request events; set a webhook secret; webhook URL = `https://<public-control-plane>/v1/ingress/github/<connection-id>` — you get the id right after connecting, then update the App), install it on the repositories, download the private key. Connections page → Connect → GitHub App flavor → paste app id / installation id / PEM / webhook secret (validated against GitHub before storing). Triggers page → New trigger → "fire on repository events" → pick the connection, repositories, events (synchronize is the opt-in cost amplifier), publish modes. Open a PR; each matching subscription runs at the exact head SHA and publishes its own attributable comment/check. For local dev without a public URL, craft payloads like `scripts/e2e-github.sh` does (sign with the webhook secret via openssl).
- **Give an agent tools (capability bundles):** Capabilities page → Register bundle (or `POST /v1/capabilities`) — sandbox servers declare `command/args/tools`, brokered servers give `url` (+ an `mcp_http` connection id when the server needs a bearer: Connections → mcp_http flavor with `base_url` + token). Registration photographs brokered tools via tools/list. Attach on the agent: Add revision → capability bundles `"name"` (pins newest now) or `"name@N"`. Narrow per trigger with the keep-list field, or per manual run with `"capabilities": [...]` on `POST /v1/sessions`. Policy still judges every call — grant `mcp__server__tool` rules deliberately (seed policy: mcp asks a human).
- **Live agent run:** ensure `ANTHROPIC_API_KEY` in `.env` + `just gateway-up`, then `cargo run -p fluidbox-cli -- run --task "…" --repo /abs/path` or use New Run in the dashboard.
- **Change the default model:** Agents page → expand `claude-fixer` → Add revision (or `POST /v1/agents/{id}/revisions`). The current (latest) revision is what runs use.
- **Rebuild the sandbox image** after editing `images/sandbox-runner/`: `just sandbox-build`.

---

## 6. Logical next steps

Ordered by leverage. The seams for all of these already exist (that's the point of the M1 architecture) — none require reworking the core.

### A. Near-term hardening — ✅ DONE 2026-07-10
All three items shipped (plan: `docs/superpowers/plans/2026-07-10-6a-hardening.md`):
1. **`just e2e`** — one command, three phases, 45 checks: `scripts/e2e.sh` orchestrates `e2e-live.sh` (demo A: live agent fixes a failing test → completed + diff + cost + isolation), `governance-e2e.sh`, `e2e-failures.sh`, over a shared `e2e-lib.sh`.
2. **Failure paths automated** (`scripts/e2e-failures.sh`): tool-call budget stop, dead-container watchdog, restart orphan sweep (reaps unknown-session containers, spares live sessions), **plus a newly-found gap fixed**: sessions stuck in `created`/`provisioning`/`initializing` after a control-plane crash are now failed by a 15-min stale-launch sweep (`Created→Failed` edge added; two real zombie rows in Neon got cleaned by it).
3. **PLAN §10 #1 + #3 resolved** from ledger data: classifier tuned (read-only utils allowed; any force-push spelling and split-flag `rm -rf /` denied) and budgets set to 1800 s / 1 M tokens / $2.50 / 100 tool calls — pinned by `seed_policy_semantics`. Bonus fix: `policy.budgets` was parsed but never read; it's now a **ceiling** at session creation. Live DB policy synced (v4).

### B. M2 — Lambda MicroVM provider + BYOC (the flagship runtime) — the big one
Implement `LambdaMicrovmProvider` behind the existing `ExecutionProvider` trait. This is where "customer-owned execution" becomes real. Key work (all validated in PLAN.md §2/§7):
- `RunMicrovm` + image pipeline (S3 zip → snapshot), in-VM supervisor serving the runner contract, JWE token minting/refresh inside `EndpointHandle` (60-min TTL → refresh loop), 8-hour lease rollover + `Harness::checkpoint()` in the orchestrator, idle-suspend/resume for long-running autonomous agents.
- Terraform BYOC module + S3 workspace/artifact store.
- **Why it's the seam it is:** `SandboxHandle` is already serializable jsonb and the `initializing` workspace phase is provider-agnostic, so nothing above the trait changes. Prove it by running the *same* demo A on the MicroVM provider with only a `runs_on: lambda-microvm` toggle.

### C. M3 — Multi-harness + capability catalog (completes the north star)
- **Codex runner:** second first-party image (`codex mcp-server` via rmcp, or the TS Codex SDK as payload). OpenAI key custody moves into the same LiteLLM gateway. This proves the harness abstraction with n=2.
- **Capability-bundle catalog:** tool/MCP bundles as first-class registry objects with credential brokering; UI to attach/narrow them per revision. Every MCP call policy-gated like any tool.
- **Customer-signed runner images** — the "bring your own agent" endgame.

### D. Roadmap — triggers & integrations (descoped from MVP)
GitHub App (webhook ingress, trigger router, fork-PR trust tier, Checks/PR-comment writers), then Slack/Jira. The seams (`sessions.trigger` jsonb, `trust_tier`, event-ingress boundary) already ship in M1 — a trigger just *creates a run of a registered agent*, so the run model doesn't change.

**Sequence (user decision 2026-07-10):** ~~A~~ done → **the "borrow the agent, on demand" axis (D + the M3 bundle piece), ahead of B/M2 MicroVMs**, following the phase order in **`docs/plans/2026-07-10-agent-workspaces-triggers-integrations-design.md` §12** (user-authored; the product/architecture direction for this whole axis).

Progress against that roadmap:
- **Phase 0 (hardening)** — ✅ done (§6.A above).
- **Phase 1 (connected git workspace)** — ✅ **shipped 2026-07-10** (see §1). Acceptance: `scripts/e2e-git-workspace.sh` (suite phase 3/4) — precedence, exact ref/commit checkout, zero-spend init failure, cleanup, live agent fix inside a `file://` clone with the remote untouched. Real-GitHub pass is manual: connect a fine-grained PAT in Connections, pick a repo on an agent, run it.
- **Phase 2 (generic API borrowing + signed result callbacks)** — ✅ **shipped 2026-07-10** (see §1; plan: `docs/superpowers/plans/2026-07-10-phase2-api-triggers.md`). Acceptance: `scripts/e2e-trigger.sh` (suite phase 4/5, 50 checks) — token scoping both directions, §17 #6 opt-in overrides + narrowing, Idempotency-Key exactly-once, frozen InvocationContext, openssl-verified signed callback, dead-destination decoupling, scoped polling, and the live §12 demo (external service borrows claude-fixer, receives one signed callback with status/summary/artifacts/cost).
- **Phase 3 (scheduled borrowing)** — ✅ **shipped 2026-07-10** (see §1; plan: `docs/superpowers/plans/2026-07-10-phase3-scheduled-borrowing.md`). **§17 #5 settled by user at the boundary: overlap default `allow` (deviating from the earlier `skip_if_running` recommendation — simpler schema, one uniform default), missed-run default `skip`, and `concurrency_policy` enforced for ALL invocations (API invokes included) inside the one `run_service::create_run`.** Acceptance: `scripts/e2e-schedule.sh` (suite phase 5/7, 54 checks) — config-time validation, sub-minute firing with frozen kind=schedule context, exactly-once across a server restart (replayed fire time → no duplicate run), overlap skip/replace with visible skips, missed skip (one row per gap) + catch_up (exactly one make-up run), API-invoke 409 gating + default-allow back-compat, signed publish, and the live §12 demo (repository-maintenance agent on a 5s cadence completes while overlapping firings skip, result delivered signed).
- **Phase 4 (GitHub PR-review fan-out)** — ✅ **shipped 2026-07-10** (see §1; plan: `docs/superpowers/plans/2026-07-10-phase4-github-pr-fanout.md`). **§17 #1–#3 settled by user at the boundary: App-only identity; default events opened+reopened with synchronize opt-in; stable results updated in place.** Acceptance: `scripts/e2e-github.sh` (suite phase 6/7, 61 checks) — App connection validated against a fake GitHub API with secrets sealed and never echoed; §17 #2 defaults pinned; dead event config refused at create; unverified deliveries 401 with nothing stored; one signed PR-opened → three isolated runs frozen at the exact head SHA with kind=event context; verbatim retry → zero new dispatches/runs/comments; fork PR → read_only frozen everywhere and probed at the REAL permission gate over the runner contract (Read/git-diff allow, Edit/compound-shell deny, ledger source=trust_tier); attributable App-identity publishes; synchronize → only the opted-in subscription fires, its comment PATCHes in place (no spam) and its check re-posts at the new SHA; seam grep (events.rs/run_service.rs contain no "github"); live tier = the §12 demo verbatim (three differently-configured agents, one PR-opened event, three isolated workspaces, three attributable published reviews). Real-GitHub pass is manual — recipe in §5.
- **Phase 5 (capability & MCP catalog)** — ✅ **shipped 2026-07-10** (see §1; plan: `docs/superpowers/plans/2026-07-10-phase5-capability-catalog.md`; research: `docs/research/2026-07-10-mcp-ecosystem-findings.md`). **§17 #7 settled by user at the boundary: pin-only** (attach resolves latest → stores the exact pin; upgrade = append a revision; no floating refs). **§17 #4 explicitly deferred** — the brokered gateway shipped now (proven on MCP); the git-write op list settles when that slice lands. Acceptance: `scripts/e2e-capabilities.sh` (suite phase 7/8, 70 checks) — mcp_http credential sealed + never echoed; registration photographs brokered tools (tools/list with the sealed bearer at the fake MCP server) and refuses poisoned/declared-brokered/collision configs; bare-name attach pins v1 and a later v2 publish moves nothing; ONE PR-opened event → four runs with four distinct frozen sets (full agent, ws-only, none, keep-list-narrowed); the REAL gate probed over the runner contract (attached+allowed, attached-but-policy-denied [attach ≠ allow], unattached server, other agent's bundle, narrowed-out bundle, drifted kb_admin — all denied with `source=capability` ledgered); the broker executes kb_search with the credential turning server-side (bearer at the fake, absent from sandbox env/RunSpec/ledger), refuses denied + wrong-class + drifted calls before egress, and ledgers `tool.brokered` with latency + digests; revoked connection fails run creation at zero spend; fork PRs freeze ZERO capabilities; live tier = the §12 demo (two agents, one event: live-kb uses the brokered kb tool THROUGH THE CONTROL PLANE + the sandbox stdio tool through the real SDK and quotes the kb result; live-plain proves the narrower frozen set never touches kb).
- **Next (user-selected 2026-07-11): "Phase 5.5" — connector catalog & OAuth custody**, ahead of Phase 6. A curated, user-selectable connector catalog (one-click api_key templates over the existing `mcp_http` flavor for GitHub/Stripe/Linear/Sentry/Atlassian) plus OAuth credential custody on connections (CIMD/DCR client identity, single `/v1/oauth/callback`, sealed rotating refresh tokens) for OAuth-only connectors (Notion; Slack later). Self-contained brief with the full design + research digest: `docs/handovers/2026-07-11-connector-catalog-session-brief.md`; research notes: `docs/research/2026-07-11-connector-catalog-oauth-findings.md` and `docs/research/2026-07-10-mcp-ecosystem-findings.md`. RunSpec/gate/photograph untouched — only credential resolution in `broker::brokered_auth` grows.
- Phases 6–7 (second harness/Codex — brief at `docs/handovers/2026-07-10-phase6-session-brief.md` — then more verticals) follow after. **Do not start any slice unprompted.**
